<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.moyu.bi.dao.DBSourceConfigurationDao">
    <insert id="addSourceConfig" parameterType="com.moyu.bi.domain.dbconfig.DBConfiguration"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO data_source_config (data_source_name, data_source_desc, url, db_name, db_password)
        VALUES (#{dataSourceName}, #{dataSourceDesc}, #{jdbcUrl}, #{dbName}, #{dbPassword})
    </insert>

    <select id="getSourceConfig" resultType="com.moyu.bi.domain.dbconfig.DBConfiguration">
        SELECT
            data_source_name,
            data_source_desc,
            db_name
        FROM
            data_source_config
    </select>
</mapper>